GET            /:service                                                     mirari.sockjs.controller.IFrameController.indexNoSlash(service)
GET            /:service/                                                    mirari.sockjs.controller.IFrameController.index(service)
GET            /:service/iframe$any<[0-9-.a-z_]*>.html                       mirari.sockjs.controller.IFrameController.iframe(service, any)
OPTIONS        /:service/info                                                mirari.sockjs.controller.InfoController.infoOpts(service)
GET            /:service/info                                                mirari.sockjs.controller.InfoController.info(service)

GET            /:service/websocket                                           mirari.sockjs.transport.WebSocketController.rootWebsocket(service)

GET            /:service/$server<[^.]+>/$session<[^.]+>/jsonp                mirari.sockjs.transport.JsonpController.jsonp(service, server, session)
POST           /:service/$server<[^.]+>/$session<[^.]+>/jsonp_send           mirari.sockjs.transport.JsonpController.jsonp_send(service, server, session)

POST           /:service/$server<[^.]+>/$session<[^.]+>/xhr                  mirari.sockjs.transport.XhrController.xhr(service, server, session)
OPTIONS        /:service/$server<[^.]+>/$session<[^.]+>/xhr                  mirari.sockjs.transport.XhrController.xhrOpts(service, server, session)
POST           /:service/$server<[^.]+>/$session<[^.]+>/xhr_send             mirari.sockjs.transport.XhrController.xhr_send(service, server, session)
OPTIONS        /:service/$server<[^.]+>/$session<[^.]+>/xhr_send             mirari.sockjs.transport.XhrController.xhr_sendOpts(service, server, session)
POST           /:service/$server<[^.]+>/$session<[^.]+>/xhr_streaming        mirari.sockjs.transport.XhrController.xhr_streaming(service, server, session)
OPTIONS        /:service/$server<[^.]+>/$session<[^.]+>/xhr_streaming        mirari.sockjs.transport.XhrController.xhr_streamingOpts(service, server, session)

GET            /:service/$server<[^.]+>/$session<[^.]+>/eventsource          mirari.sockjs.transport.EventSourceController.eventsource(service, server, session)
GET            /:service/$server<[^.]+>/$session<[^.]+>/htmlfile             mirari.sockjs.transport.HtmlFileController.htmlfile(service, server, session)
GET            /:service/$server<[^.]+>/$session<[^.]+>/websocket            mirari.sockjs.transport.WebSocketController.websocket(service, server, session)



GET            /                                                             mirari.sockjs.controller.TestController.index

GET            /*any                                                         mirari.sockjs.controller.IFrameController.nofound(any)
POST           /*any                                                         mirari.sockjs.controller.IFrameController.nofound(any)

