GET            /:service                                      mirari.sockjs.Application.indexNoSlash(service)
GET            /:service/                                      mirari.sockjs.Application.index(service)
GET            /:service/iframe$any<[0-9-.a-z_]*>.html         mirari.sockjs.Application.iframe(service, any)
OPTIONS        /:service/info                                  mirari.sockjs.Application.infoOpts(service)
GET            /:service/info                                  mirari.sockjs.Application.info(service)
OPTIONS        /:service/chunking_test                         mirari.sockjs.Application.chunking_test(service)
POST           /:service/chunking_test                         mirari.sockjs.Application.chunking_test(service)
GET            /:service/websocket                             mirari.sockjs.Application.rootWebsocket(service)

GET            /:service/:server/:session/jsonp                mirari.sockjs.Application.jsonp(service, server, session)
POST           /:service/:server/:session/jsonp_send           mirari.sockjs.Application.jsonp_send(service, server, session)
POST           /:service/:server/:session/xhr                  mirari.sockjs.Application.xhr(service, server, session)
OPTIONS        /:service/:server/:session/xhr                  mirari.sockjs.Application.xhrOpts(service, server, session)
POST           /:service/:server/:session/xhr_send             mirari.sockjs.Application.xhr_send(service, server, session)
OPTIONS        /:service/:server/:session/xhr_send             mirari.sockjs.Application.xhr_sendOpts(service, server, session)
POST           /:service/:server/:session/xhr_streaming        mirari.sockjs.Application.xhr_streaming(service, server, session)
OPTIONS        /:service/:server/:session/xhr_streaming        mirari.sockjs.Application.xhr_streamingOpts(service, server, session)
GET            /:service/:server/:session/eventsource          mirari.sockjs.Application.eventsource(service, server, session)
GET            /:service/:server/:session/htmlfile             mirari.sockjs.Application.htmlfile(service, server, session)
GET            /:service/:server/:session/websocket            mirari.sockjs.Application.websocket(service, server, session)

GET            /*any                                           mirari.sockjs.Application.notFound(any)